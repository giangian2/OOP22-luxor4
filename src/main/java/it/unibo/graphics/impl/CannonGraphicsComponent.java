package it.unibo.graphics.impl;

import it.unibo.model.GameObject;

public class CannonGraphicsComponent implements GraphicsComponent {
    @Override
    public void update(GameObject obj, Graphics c) {
       
        //qui non so che scrivere (quello sotto non va bene)
       // c.setColor(Color.RED);
        //c.fillOval((int) obj.getPos().x, (int) obj.getPos().y, (int) obj.getRadius(), (int) obj.getRadius());
    }
}
